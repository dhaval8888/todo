npm init
npm i express
copy code from express web page ---> node filename to run
express npm

  "type": "module",
  import express from "express";

npm i nodemon ----> so you dont have to start backend evrey time you edit code. it will be authometically run

add nodemon start in nodepackge

create cluter in mongodb

// to conect mongodb
try{
    mongoose.connect(db_uri);
    console.log("mogngodb connected");
}
catch(error){
    console.log(error);
}

create schemass in new .js file
schemas are improted frome mongoose
then create model of that schema
            const todoschema= new mongoose.Schema({
                title:{
                    type:String,
                    required:true
                },
                completed:{
                    type:Boolean,
                    required:true
                }

            })

create controller 
    create function to create todo
        export const createTodo = async (req, res) => { // creating a function
            const todo= new Todo({
                title: req.body.title,
                completed: req.body.completed
            });

            try{
                const newtodo= await todo.save();
                return res.status(201).json({message:"todo creared",newtodo})
            } catch(error){
                console.log(error)
                return res.status(400).json({message:"error has been occered"})
            }
        }
    function to fatch todos

create route file
    route created todo
    route fatch

